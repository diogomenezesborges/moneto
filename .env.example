# ============================================================
# Moneto Environment Configuration
# ============================================================
# Copy this file to .env and fill in your values.
# Variables marked [REQUIRED] must be set for the app to start.
# Variables marked [OPTIONAL] enable additional features.
# ============================================================

# --- Core Database (Required) --------------------------------
# PostgreSQL connection string from Neon (https://neon.tech)
# Sign up free, create a project, copy the connection string.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Direct connection URL for Prisma migrations
# In Neon dashboard: Connection Details -> Direct connection
DIRECT_URL="postgresql://user:password@host/dbname"

# --- Authentication (Required) -------------------------------
# Secret for signing JWT tokens. Must be at least 32 characters.
# Generate with: openssl rand -base64 32
JWT_SECRET="generate-a-random-secret-at-least-32-chars"

# Secret for CSRF token validation. Must be at least 32 characters.
# Falls back to JWT_SECRET if not set, but a separate value is recommended.
# Generate with: openssl rand -base64 32
CSRF_SECRET="generate-a-separate-csrf-secret-32-chars"

# Public URL where the app is deployed (no trailing slash).
# Use http://localhost:3000 for local development.
NEXTAUTH_URL="http://localhost:3000"

# --- AI Categorization (Optional) ----------------------------
# Google Gemini API key for AI-powered transaction categorization
# Get one free at: https://aistudio.google.com/apikey
# GEMINI_API_KEY="your-gemini-api-key"

# --- Rate Limiting (Optional) --------------------------------
# Upstash Redis for API rate limiting.
# Free tier available at: https://console.upstash.com
# Create a Redis database, copy REST URL and token.
# UPSTASH_REDIS_REST_URL="https://your-redis.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# --- Logging (Optional) --------------------------------------
# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL="info"

# --- Runtime (Automatic) -------------------------------------
# NODE_ENV is set automatically by Next.js.
# Do not set manually unless you know what you are doing.
# NODE_ENV="development"
